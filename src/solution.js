/**
 *
 * @param {number[]} arr - a sequence of elements (can be integers, strings, or other types)
 * @param {number} n - a number or a target element (based on problem context)
 * @return {number}
 *
 * This function will be called by our system.
 * So fill in your code inside this function.
 *
 */
module.exports = function handle(arr, n) {
  // WRITE YOUR CODE HERE....
  return arr; 
};
